# Matched User Profile Page - Task List

## ✅ Backend
- [x] Update `GET /:matchId` in `mates_backend/routes/matchRoutes.js` to populate full user profile fields

## ✅ Frontend
- [x] Create `mates_frontend/src/app/matches/[userId]/page.js` — Matched user profile page with image gallery, info, interests
- [x] Update `mates_frontend/src/app/matches/page.js` — Split card into left (profile) and right (chat) clickable zones

